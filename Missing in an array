class Solution {
    int missingNum(int arr[]) {
        // code here
        int sum=0;
        for(int i=0;i<arr.length;i++)
        {
            sum=sum+arr[i];
        }
        long n=arr.length+1;
         long total=n*(n+1)/2;
       
        return (int)(total-sum);
    }
}
